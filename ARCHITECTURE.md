# Architecture — where code lives

Maintained by the master in docs PRs (owner ruling 2026-09-08). Condensed from the factory's structure map (frozen at `archive/sdlc-factory-2026-09-04/corpus/docs/registry/structure.md`). A new top-level directory or a new `src/lib/` module is a PR to this file first. The documentation corpus and its authority order are `docs/README.md`.

Standard Next.js (App Router) + TypeScript. `src/lib/` is the engine, `src/app/` the surfaces, `src/jobs/` the scheduled work, `src/ui/` the design system, `supabase/` the schema. Every pinned number is in `src/lib/config/constants.ts`.

## Modules

| Path | Responsibility | Public entry points | BUILD · DECISIONS |
|---|---|---|---|
| `src/app/(public)/**` · `(account)/**` · `api/**` · `layout.tsx` · `middleware.ts` | The application container — every surface a person reaches, and thin HTTP adapters that delegate to the engine. Holds no engine logic. | `/` · `/scan/{domain}` · `/pricing` · `/setup` · `/app` (Overview · Calendar · Settings) · `/app/draft/{id}` · `api/**` | §3, §4 |
| `src/app/(public)/layout.tsx` · `src/app/(public)/_chrome/**` | The public shell — one header (brand · quiet Sign in · one solid CTA; *Copy link* on the report) and one footer (brand · rights line · removal address · Product · Legal) around every `(public)` route, rendered by the group layout. **Approved** (owner ruling 3a, 2026-09-08; UI-SPEC §1). Two solid primaries are allowed where the approved set draws them (2b); the header's CTAs on the landing focus the hero field (REQ-099 c3). Every sentence is a copy key (`chrome.*`, `legal.*` titles). | `PublicLayout` · `Header` · `Footer` | §3 · 2026-09-08 rulings 2b, 3a |
| `src/app/(public)/privacy/**` · `terms/**` · `imprint/**` | The three static legal routes the footer links to, so a footer link reaches a page rather than a 404. Each renders exactly two owner-owed strings, a title and a body, and drafts neither: a privacy statement, a set of terms or an imprint written by anything but the owner is a legal claim nobody made. Both keys carry `TODO(copy)`, which renders — rule 8, `tests/presentation/copy/string-literal-sweep.test.ts` — so the pages and their chrome are reviewable while the words are still owed. Server Components that read no session, no cookie and no store. | `/privacy` · `/terms` · `/imprint` | §3 · #266 |
| `src/app/(hosted)/**` | Hosted-content edge — Host-keyed static rendering of published pages on `content.{customer-domain}`, plus the robots/sitemap/410 policy served there. | `GET /*` by Host · `/robots.txt` · `/sitemap.xml` | §9, §11 |
| `supabase/**` · `src/lib/db/**` | Schema, migrations, RLS default-deny, and the only two clients any code may hold. | `db()` (RLS) · `dbAdmin()` (server-only) | §10 |
| `src/jobs/**` | The job runner — scan/run · draft/generate · publish/execute · publish/verify · weekly/refresh (hourly tick, site-local Monday) · lead/nurture · account/maintenance — and the kill switch. | job registry · `serve()` | §11 |
| `src/lib/config/**` | Every pinned number and env binding, defined once, asserted once. | `constants.ts` · `env.ts` · `tests/pins.test.ts` | §6.1, §15 |
| `src/lib/egress/**` | The one way any byte leaves toward a customer- or dataset-supplied URL: SSRF-guarded, DNS-pinned, size-capped, robots-respecting. | `safeFetch()` · `resolvesInDns()` · `readRobots()` | §6.4 |
| `src/lib/costs/**` | The cost seam — every vendor/model call ledgered, cached and cap-checked; `fetches` is ledger + cache + raw store. | `withCostContext()` · `recordFetch()` · `capHit()` · `readCache()` | §6.5 |
| `src/lib/vendors/**` | The closed list of DataForSEO endpoints, and nothing else. | `rankedKeywords()` · `keywordSuggestions()` · `competitorsDomain()` · `serpOrganic()` · `aiMode()` · `llmScraper()` | §6.1–6.3 |
| `src/lib/llm/**` | The one `llm()` seam — nano + Haiku, per-call cost logging, closed call-site list: the sites are the keys of `INFERENCE_MAX_OUTPUT_TOKENS` (`src/lib/config/constants.ts`), typed as `LlmCallSite = keyof typeof INFERENCE_MAX_OUTPUT_TOKENS`; there is no `LLM_CALL_SITES` export (row corrected 2026-09-10). | `llm()` · `LlmCallSite` · `INFERENCE_MAX_OUTPUT_TOKENS` | §1, §6.5, §8 |
| `src/lib/measure/**` | Deterministic parse of fetched HTML into measured quantities, score factors, band, and the measured / measured-zero / unmeasured trichotomy. | `measureDomain()` · `computeScore()` · `Measured<T>` | §5 |
| `src/lib/market/**` | Business profile, market set, the 12 questions, coherence, rival derivation and sizing. | `deriveProfile()` · `deriveMarketSet()` · `selectTwelve()` · `phraseQuestions()` · `deriveRivals()` · `sizeRivals()` | §6.6, §6.7 |
| `src/lib/scan/**` | The one scan pipeline (tier is a parameter), free-path admission, time/spend ceilings, degradation, the stored report. | `runScan()` · `admitFreeScan()` · `readCurrentReport()` | §4.1, §6.3 |
| `src/lib/opportunities/**` | The eight opportunity types, evidence, acceptance tests, winnability, ranking, supply depth, weekly verdicts. Gains the Earn family (`listed_page`), parent-topic clustering with `absorbed_queries`, the Improve-outranks-Write precedence sort, the `keyword_page` gates and the readiness predicate that `next.ts` and `supplyDepth()` obey (brief 2026-09-10; lands with #473 pins and types, #474 the deriver, #477 verdicts, #478 Earn). | `deriveOpportunities()` · `bandWinnability()` · `supplyDepth()` · `judgeWeek()` · `readWeek()` · `weeklyDigest()` · `clusterKey()` · `opportunityReady()` | §7 · 2026-09-10 rulings §1.2–§1.7, §1.10 |
| `src/lib/generate/**` | brief → outline → grounded draft → answerability pass → claim check; every hard rule enforced in code. Gains hard rules 8–12 (no invented tests, no fake author, no new question headings from the pass, pass evidence traceable to brief facts, first block answers the question) inside `runHardRules()` / `claimCheck()`, the closed per-type outline skeletons, and the brief-facts-or-no-Haiku gate (brief 2026-09-10; lands with #475). | `generateDraft()` · `runHardRules()` · `claimCheck()` | §8 · 2026-09-10 ruling §1.11 |
| `src/lib/publish/**` | State machine, veto/approval rule, destination adapters, idempotency, retries, health, 24h verification, page record. | `transition()` · `becomesPublishable()` · `publish()` · `verifyLive()` · `destinations/**` | §9 |
| `src/lib/mail/**` | One `sendEmail()`, one branded shell + plain-text twin, the register of mail kinds and their stoppability, sequence scheduling. | `sendEmail()` · `MAIL_KINDS` · `scheduleSequence()` | §12 |
| `src/lib/account/**` | Stripe checkout/webhook/portal, provisioning, identity on Supabase Auth (`identityAuth()` port: admin `generateLink` → `/auth/confirm` → `verifyOtp`, `@supabase/ssr` session, server-verified `getUser()`; 2026-09-10, #468), active access, export, unpublish-all, erasure. | `handleStripeWebhook()` · `provisionFromPayment()` · `hasActiveAccess()` · `exportEverything()` · `deleteAccount()` | §13 |
| `src/lib/presentation/**` | The copy registry (every sentence the product speaks), rendering of `Measured<T>`, the cold-start line, the generated-text brand, the stopped-work statement, band labels. | `copy()` · `renderMeasured()` · `GeneratedText` · `BAND_LABELS` | §2.5 |
| `src/ui/**` · `tests/ui/**` | Design system — daisyUI theme tokens, the registered component set, closed chart inventory, fonts, the three layout bands and `Surface`, the layout conformance suite. | `theme.css` · `components/**` · `charts/**` · `layout/**` | §2 |
| `src/ui/idiom/**` | The card idiom as the approved set draws it: card head slot (chip · eyebrow · right slot), the idiom card skin on `--r-box` 14, the tinted ActionPanel, the pill ranks (solid · outline · quiet · warn). Radius 14 and mono numerals are ruled (8a, 7a). Not a sixteenth daisyUI component and not a sixth custom-CSS surface (registry test). | `IdiomCard` · `CardHead` · `ActionPanel` · `idiom.css` | §2.1–§2.3 · UI-SPEC §2 |
| `src/ui/layout/surface.css` | ADR-093's rendering half — the sheet that places a screen root: a centred column, per-band gutters from the ruled spacing scale, width capped at `--w-read` 704 or `--w-wide` 1216, and a `columns:N` arm rendered as a grid inside its own band's query. It exists because `Surface.tsx` wrote `data-surface` and `data-arm-<band>` and no stylesheet in the bundle answered them, so every screen root rendered flush to the top-left while conformance checks 1–4 passed; check 5 (`tests/ui/layout/checks.ts`) is the half that was missing. Not one of §2.2's five custom-CSS surfaces: it styles no component and names no daisyUI class, and the path glob in `tests/ui/design/component-registry.test.ts` is what says so. | `[data-surface]` · `[data-arm-<band>]` | §2.2–§2.3 · ADR-093, #241, #258 |
| `docs/**` | The documentation corpus: `README.md` (authority order), `PROCESS.md`, `DEPLOYMENT.md`, `pending-decisions.md` (the master's ruling queue), `design-reference.md`, `RUNBOOK.md` (not yet written — #333), and `design/approved/**` — the owner's artifact, `tokens.css` (token source of truth for `theme.css`), and `full-set/` (the approved screen set + `UI-SPEC.md`). Nothing under `docs/` is read by the running product. | `docs/README.md` · `docs/design/approved/full-set/UI-SPEC.md` | §0, §2, §4 · 2026-09-08 |
| `src/app/(public)/pricing/**` · `veto/**` · `opt-out/**` · `not-found.tsx` · `error.tsx` | The remaining public surfaces: the one offer page (REQ-021 c4), the veto page a draft-ready mail links to (REQ-057), the opt-out confirmation (REQ-010 c11), and the 404/error pages — each one card in the idiom with the public chrome. | `/pricing` · `/veto/{token}` · `/opt-out/{token}` | §3 · UI-SPEC S4, S6, S7, S8 |
| `scripts/**` | Repository tooling, owner-file: `db-substrate/**` (the live Postgres + PostgREST + postgres-meta stack that CI and every worktree run the `db` and `layout` suites against — `up.sh` · `down.sh` · `reap.sh` · `gen-types.sh`), `drift-audit.mjs` (spec ↔ code ↔ tests), `land.sh` (the master's landing chain, PROCESS §3). Tooling against the real project (`scripts/db-live/**`: migration apply, live smoke runs) is M11 work and does not exist yet. | `up.sh` · `drift-audit.mjs` · `land.sh` | §15, §16 · PROCESS §3, §5 · M11 |

## Rules (each has a check; the check is the rule)

1. **`src/app/**` holds no engine logic.** Route handlers and server components are thin adapters over a module's exported interface. *(review)*
2. **Dependency direction is one-way:** `src/app/`, `src/jobs/` → `src/lib/` → `src/lib/{config,egress,costs,db}`. `src/lib/**` never imports `src/app/**`. *(eslint `local/no-lib-importing-app`)*
3. **Module boundaries are import fences:** only `@/lib/db` outside `src/lib/db`; only `hasActiveAccess()` outside `src/lib/account/billing`; `account/export` never imports `account/billing`. *(eslint `local/no-db-internal-import`, `no-billing-internal-import`, `no-export-importing-billing`)*
4. **No `fetch(` under `src/lib/**` outside `egress/` and `vendors/`.** *(eslint `local/no-fetch-outside-egress`)*
5. **Config over constants.** A number in two files is wrong; it belongs in `constants.ts`, asserted in `tests/pins.test.ts`. *(pins test; `tests/config/constants.test.ts`)*
6. **Migrations are topic-prefixed** — `users` · `sites` · `scans` · `fetches` · `opportunities` · `drafts` · `publications` · `destinations` · `leads` · `suppressions` — with optional sub-tokens (`*_users_subscription_*`). The baseline runs once; later columns are their own migration. *(`tests/db/migration-naming.test.ts`)*
7. **The committed top-level set is closed:** `src/ supabase/ tests/ public/ scripts/ docs/ archive/ .github/ .claude/`. *(`tests/app/toolchain.test.ts`)*
8. **Every sentence the product speaks is a copy key; generated text is branded.** *(`tests/presentation/copy/string-literal-sweep.test.ts`, `generated/flow-gate.test.ts`)*
9. **Tests live beside the module** — `tests/<module>/**`. Journey tests live in `tests/journeys/`, one per arrow in BUILD §3. *(`scripts/drift-audit.mjs`)*
10. **A module marks the spec it implements** with `// BUILD §x.y` at the top of the file. *(`scripts/drift-audit.mjs`)*
11. **Every route has a row in `docs/design-reference.md`** naming its `S<id>`, UI-SPEC section, REQ criteria and tests. *(a test walks `src/app/**` and the table)*
12. **No bare design literal.** Colour, radius, shadow, spacing, type size, measure and breakpoint values in `src/ui/**` and `src/app/**` are `var(--…)` from `theme.css`, which equals `docs/design/approved/tokens.css`. *(`tests/ui/design/token-set.test.ts`, `no-bare-literals.test.ts`)*
