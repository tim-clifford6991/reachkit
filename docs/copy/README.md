# Copy owed

`npm run copy:owed` writes `owed.md` here for the owner to fill. That file is gitignored: it is a worksheet, not a merge artifact.

Sentences go in `src/lib/presentation/copy/keys/`. `TODO(copy)` on a screen; mail with an empty key does not send.
