# M3b — write-only producer cut: surgical spec (agent-verified, 2026-07-23)

> Execution spec for launch plan L2. Owner decisions O-7 (reviews both tiers) / O-8 (creators) / O-9 (content-intel only-where-it-feeds-plan) are already recorded in REQUIREMENTS §0.1. **28 deletions · 30 edits · 5 guard updates.** Every reference below was verified at file:line.

**Load-bearing fact:** `competitiveLandscape`, `creatorsToReach`, `strengthsAndWeaknesses` have ZERO render consumers (grep of `components/`+`app/` returns nothing) — produced, redacted, diagnostic-mapped, never displayed. `reviewThemes` is NOT write-only: it feeds LLM prompts + `icpSignals`, so its cut is a **prompt-shape change**, not a pure deletion.

## Producer 1 — reviews / reviewThemes (O-7)
- **Collect:** `collect.ts:12` import + `:70-101` `reviewsPromise` branch → `Promise.resolve({reviews:[]})`; `:130-136` web ratingCount block DELETE; comment `:73` update.
- **Extract:** `extract.ts:9,16` imports + `:197,218-219` review_themes job DELETE.
- **full-scan:** DELETE `readIcpSignals` (`:124-139`) + `readReviewThemesFull` (`:408-436`); `:622-626` Promise.all → `readSurfaces` only; drop `icpSignals`/`reviewThemes` args (`:635,643`).
- **Types:** DELETE `ReviewThemesSheet` (`llm/types.ts:109-117`), `EMPTY_REVIEW_THEMES` (`:144`); remove `"review_themes"` from `FactSheetKind` (`fact-sheet-kind.ts:15`). KEEP `PositioningMirror.reviewsValue` (still rendered via `report.ts:287 buildWhoSummary`, now always `""`) + `ReviewItem` (monitor uses it).

## Producer 2 — strengthsAndWeaknesses (O-7, write-only)
DELETE: types `ReviewTheme`/`StrengthsAndWeaknesses` + payload field (`report.ts:73-84,138`); assembleReport param/default/return-block (`:256,275,299-310`); redaction `redactStrengths`+`FREE_PREVIEW_THEMES`+wiring (`entitlements.ts:38,191,303-314`); diagnostics point (`diagnostics.ts:140`).

## Producer 3 — creators / find-creators / youtube / audienceProxy (O-8)
- **DELETE whole files:** `tools/find-creators.ts`, `adapters/youtube.ts` (+ their tests).
- **full-collect.ts:3,44-46** remove findCreators import + settled call. **tools/index.ts:8,22,29** unregister.
- **full-scan:** DELETE `readCreatorDocs` (`:267-302`); `:542` Promise.all → `[competitorGap, channelOpportunities]` (**both STAY**); `:547-556` drop `grounding.creators`; `:642` drop assembleReport arg.
- **grounding.ts:8,16,19** drop `creators` field + `CreatorReach` import + `EMPTY_GROUNDING.creators`. **cold-start-actions.ts:306,369** drop creator seed.
- **Types:** DELETE `CreatorReach` (`report.ts:65-71`, +field/param/default/return) + `Creator` (`types.ts:35`). Redaction `FREE_PREVIEW_CREATORS`+line (`entitlements.ts:37,190`). Diagnostics `:139`.

## Producer 4 — competitiveLandscape (write-only)
DELETE `buildCompetitiveLandscape` (`full-scan.ts:438-460`) + call `:627` + arg `:640` + import `:48`; type `CompetitiveLandscapeRow`+field (`report.ts:24-38,132` +param/default/return); redaction `redactLandscape`+wiring+import (`entitlements.ts:11,188,270-282`); diagnostics `:137`.

## The prompt-shape change (the risky part)
Three prompts inject `=== REVIEW THEMES SHEET ===`; after the cut the sheet is always empty → dead tokens.
- **buildActionsPrompt** (`prompts.ts`): drop `reviewThemes` param (`:167`) + section (`:211-212`); drop creators block (`:189-201`); drop `review_themes`/`youtube` from sourceType enums (`:159,234`) + "or CREATORS" (`:161`); rule 4 → "…from the competitor gap" only.
- **buildSynthPrompt** (`:279-341`): drop `reviewThemes` param+section (`:281,295-296`); `reviewsValue` schema → hard-require `""` (`:308-309`); drop `review_themes` from findings source enum (`:321`).
- **buildSynthPromptLite** (`:360-407`): drop `reviewThemes` (`:361,371-372`); `reviewsValue` hard-`""` (`:381-382`); `SYNTH_SYSTEM_LITE` rule 2 (`:357`).
- **Callers:** `actions.ts:265-286` drop read + prompt arg; `synth.ts:285-309` drop read + args, and make the empty-review wipe at `:329-342` **UNCONDITIONAL** (`reviewsValue=""; actualAudience=[]` — preserves invariant #11 at synth, matches `grounding.test.ts`); `refresh.ts:396-447` drop review_themes read+section in `synthNovelFindings`.
- **Prompt-shape tests to update same-change:** `synth.model.test.ts:30-48`, `actions.test.ts:13`, `extract.test.ts:86`.

## Weekly-refresh / deepen cohesion
Deepen IS the "Phase E moment": `scan-deepen.ts` → `runFullScan` reuses stored facts; after the cut it stops touching reviews/creators — no deepen-specific edits beyond the shared `full-scan`/`full-collect` cuts. Weekly-refresh reads review_themes at `refresh.ts:421-447` (covered by the prompt-D edit) + via `generateActions` (covered by `actions.ts`). **score-pulse: no edits.**
**FLAG (owner, default KEEP):** the `reviews` MonitorKind (`types.ts:39`, `monitors.ts`, `delta-collect.ts` `collectReviews`→`fetchAppReviews`→`app-store-rss.ts`) is app-store-RSS review *deltas* for the weekly "what changed" digest — a SEPARATE producer from `review_themes`. KEEP unless O-7 is extended to it (then `app-store-rss.ts` + `ReviewItem` also die).

## Grounding machinery (invariant #11): KEEP dropDomainConflicts
`web-reviews.ts` is TRIMMED not deleted: CUT `fetchWebReviews`/`filterSubjectResults`/`parseWebReviewSnippets`/`reviewCountFromSnippets`/`GROUNDING_POLICY_VERSION`; **KEEP `referencedDomains`+`dropDomainConflicts`+`TavilyResult`** (news/buzz, verified used by `gap/run.ts:71`). `fact-sheets.ts`: drop `GROUNDING_POLICY_VERSION` import + `review_themes` policy-suffix line (KEEP `relevance_verdicts`). Tests: `grounding.test.ts` reduces to the unconditional-wipe assertion; `web-reviews.test.ts` keeps only `dropDomainConflicts`; `fact-sheets.test.ts` drops review-TTL/policy tests.

## Guard updates (5)
1. **G9 paid-data ledger** — ONLY if O-9 cuts `gatherContentIntel`: `cachedRelevantPages` orphans → delete ledger line 29 + the wrapper. (Reviews/creators tools are cost-0, not in G9.)
2. **capability-ledger** — `collect.ts` keeps `discoverScanCompetitors` → passes; verify the collect edit doesn't remove that call.
3. **report-corpus rubric** — the 7 fixtures still CARRY these keys (legacy); KEEP JSON verbatim; renderer never reads them post-type-removal → legacy-render guard still passes (null-coalesce: PASS).
4. **CLAUDE.md** invariants #3 + #11 prose (describe now-dead GROUNDING_POLICY_VERSION/filterSubjectResults) + **architecture.md:228** (Creators row) + **:739** (adapter list) EDIT.
5. **tool-registry** — verify no completeness test enumerates `get_reviews`/`find_creators` (none found).

## O-9 content-intel (enumerated, not decided)
`gatherContentIntel` (`content/gather.ts`) is imported ONLY by the two intel routes + two test harnesses; renders ONLY in the **unmounted** SupplyView `ContentEngine`; never reaches `plan/generate` (which uses `gatherSynthesis.contentPlan`, a different module). → It IS the standalone content surface O-9 targets. **Cross-ref the data-contract map:** cutting it also kills waste finding #8 and pairs with gating the orphaned `supply`/`demand` sub-gathers.

## Sequencing note
This spec + the data-contract map together mean **L2/M3b should ALSO gate the four M3a-orphaned Pipeline-B gathers** (`keywords.gaps`, `buyerInsights`, `searchDemand`, `synthesis.summary`) out of the mounted intel endpoints and delete the unmounted supply/demand/synthesis views + dead `buildExecutiveSummary`/`competitive-framing.ts`. Same PR, same deep-scan validation — turns M3b from "cut dead payload fields" into "every cold paid tab load gets dramatically cheaper."
