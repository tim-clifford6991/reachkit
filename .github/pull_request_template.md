Closes #

## What changed
<!-- one paragraph -->

## Evidence
<!-- the command run, the route looked at, what it showed -->

## Corpus
<!-- the documented fact this PR changes — named here, landed by the master — or "nothing" -->

<!-- A token table (every value → its token) only if tokens changed.
     A `Renders:` line naming routes only if a screen changed.
     Adjacent findings go under an *Adjacent* heading. -->
